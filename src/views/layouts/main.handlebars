<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pc-Componentes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body style="padding: 60px" class="bg-secondary p-2 text-dark bg-opacity-10">
  <nav class="navbar navbar-expand-lg bg-body-tertiary fixed-top">
    <div class="container-fluid">

      <a class="navbar-brand" href="/">Pc-Componentes</a>

      <div class="d-flex ms-auto align-items-center">
        {{#if user}}

          {{#if isAdmin}}
            <a href="/realTimeProducts" class="btn btn-outline-warning btn-sm me-2">
              ⚙️ Productos en tiempo real
            </a>
          {{/if}}

          <a href="/current" class="btn btn-outline-info btn-sm me-2">
            👤 Mi perfil
          </a>

          {{#if user.cart}}
            <a href="/carts/{{user.cart._id}}" class="btn btn-outline-primary btn-sm me-2">
              🛒 Ver carrito
            </a>
          {{/if}}

          <form action="/api/sessions/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-danger btn-sm">Cerrar sesión</button>
          </form>

        {{else}}

          <a href="/login" class="btn btn-outline-primary btn-sm me-2">Iniciar sesión</a>
          <a href="/register" class="btn btn-outline-success btn-sm">Registrarse</a>

        {{/if}}
      </div>
    </div>
  </nav>

  {{{body}}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>