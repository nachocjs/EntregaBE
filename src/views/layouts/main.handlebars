<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Pc-Componentes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-secondary bg-opacity-10 text-dark p-2" style="padding-top: 70px;">
  
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top shadow-sm">
    <div class="container-fluid">
      <!-- Marca -->
      <a class="navbar-brand fw-bold fs-4 text-dark" href="/">Pc-Componentes</a>

      <!-- Botones a la derecha -->
      <div class="d-flex ms-auto align-items-center flex-wrap gap-2">

        {{#if user}}
          {{#if isAdmin}}
            <a href="/realTimeProducts" class="btn btn-outline-warning btn-sm">
              ⚙️ Productos en tiempo real
            </a>
          {{/if}}

          <a href="/current" class="btn btn-outline-info btn-sm">
            👤 Mi perfil
          </a>

          {{#if user.cart}}
            <a href="/carts/{{user.cart._id}}" class="btn btn-outline-primary btn-sm">
              🛒 Ver carrito
            </a>
          {{/if}}

          <form action="/api/sessions/logout" method="POST" class="d-inline">
            <button type="submit" class="btn btn-outline-danger btn-sm">Cerrar sesión</button>
          </form>

        {{else}}
          <a href="/login" class="btn btn-outline-primary btn-sm">Iniciar sesión</a>
          <a href="/register" class="btn btn-outline-success btn-sm">Registrarse</a>
        {{/if}}

      </div>
    </div>
  </nav>

  <!-- Body dinámico -->
  {{{body}}}

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>